<view>
	<van-popup z-index="1000000" show="{{ show }}" position="bottom" bind:close="onClose">
		<van-datetime-picker
			type="time"
			value="{{ currentDate }}"
			filter="{{ filter }}"
		/>
	</van-popup>
</view>